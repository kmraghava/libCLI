# libCLI
A simple library to build and maintain Command Line Interface.
This is still work in progress ...

# Build libxmlc
```bash
git clone git@github.com:kmraghava/libxmlc.git
cd libxmlc

make
sudo make install
```
